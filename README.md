# GrihNirmaan — Render Docker Package

This repository contains:

- `frontend/`: standalone React + Vite frontend
- `backend/`: Laravel + Filament backend
- `Dockerfile`: one-container Render deployment
- `render.yaml`: Render Blueprint configuration
- `RENDER-DEPLOY.md`: complete deployment steps

Read **RENDER-DEPLOY.md** before deploying.
